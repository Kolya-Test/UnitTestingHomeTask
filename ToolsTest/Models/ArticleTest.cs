using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using NUnit.Framework;
using Xunit;
using msAssert = Microsoft.VisualStudio.TestTools.UnitTesting.Assert;
using nAssert = NUnit.Framework.Assert;
using xAssert = Xunit.Assert;
using Theory = Xunit.TheoryAttribute;

namespace ToolsTest.Models
{
    // Написати тести для класу Article за допомогою будь-якого фремворку зручного для вас.
    // * Використовуючи клас msAssert буде використано MSTest, якщо ж nAssert то nUnit, ну і якщо xAssert то xUnit.

    public class ArticleTest
    {
    }
}
